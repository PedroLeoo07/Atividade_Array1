let numero = [200, 300, 400, 500, 2];
//Let criada para verificação dos numeros

let dobro = numero.map (function (numero) {
    return numero * 2;
});
console.log (dobro);
//Console criado parao dobro do número